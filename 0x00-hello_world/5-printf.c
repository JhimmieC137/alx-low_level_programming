#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char answer[] = "with proper grammar, but the outcome is a piece of art,"

	printf(answer);

	return (0);
}
