#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Going far
 *
 * Return: Alwasy 0 (success)
 *
 */

int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
		if (n != 57)
		{
			putchar(',');
			putchar('');
		}
	}
	putchar('\n');
	return (0);
}
